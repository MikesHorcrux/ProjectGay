import SwiftUI

/// Design system from the product doc: warm neutrals, Pride accents as highlights.
enum Theme {
    /// Primary actions (e.g. RSVP).
    static let coralRose = Color("CoralRose")
    /// Calendar and scheduling cues.
    static let skyTeal = Color("SkyTeal")
    /// Tags and soft delight moments.
    static let lavenderMist = Color("LavenderMist")
    /// Default canvas.
    static let cream = Color("Cream")
    /// Default text.
    static let softCharcoal = Color("SoftCharcoal")
}
